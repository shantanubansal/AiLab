[project]
name = "hello-llm"
version = "0.1.0"
description = "AiLab python-llm starter template."
requires-python = ">=3.11"
dependencies = [
  "anthropic>=0.40.0",
]

[tool.setuptools]
py-modules = ["agent"]
